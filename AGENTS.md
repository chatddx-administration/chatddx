Devenv in flake.nix devShell
Assume database is always PostgreSQL 17
Use `pytest -m "not network"` as baseline
Never put help_text or any other user facing data in django models or migrations
`chatddx repl` exists
