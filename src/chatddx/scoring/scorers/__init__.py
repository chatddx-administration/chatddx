"""
chatddx's own scorers. A scorer file imports nothing else of chatddx's, so
the file is all of chatddx that scored, and its git blob id names the
version each score records (`runtime.implementation`).
"""
