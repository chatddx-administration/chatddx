extends = ["connections.toml"]

[agent.no-thinking]
instructions = ""
connection = "qwen3-8b"
sampling_params = ["seed-locked", "disable-thinking"]

[sampling_params.seed-locked]
seed = 0

[sampling_params.disable-thinking]
provider_params_path = "./disable_thinking.json"
