"""
chatddx's runs as inspect-ai's eval logs (data-generation.md §5, option C):
chatddx sends the requests, and inspect shows, scores and reads what came
back. Everything here imports inspect, which takes seconds, so the repl
imports it only when asked to write a log.
"""
