# The inventory, all but its case corpus, and two cases of the tests' own.
extends = [
  "../../../data/inventory/pelle.toml",
  "../../../data/inventory/malborg.toml",
  "../../../data/inventory/models.toml",
  "../../../data/inventory/clients.toml",
  "../../../data/inventory/slices.toml",
  "../../../data/inventory/configurations.toml",
]

[case.case-1]
payload = "case payload 1"
tags = ["tag-1", "tag-2"]

[case.case-2]
payload = "case payload 2"
