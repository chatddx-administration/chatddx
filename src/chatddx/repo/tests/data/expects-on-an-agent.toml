# `expects` is a case's to carry. Naming it on an agent used to be accepted
# by the shared details model and then dropped; see test_registry.py.
extends = ["test-inventory.toml"]

[agent.agent-with-expects]
instructions = "hello"
connection = "connection-1"
output_type = "output_type-1"
tool_group = "tool_group-1"
sampling_params = "sampling_params-1"
expects = ["expect-1-a"]
