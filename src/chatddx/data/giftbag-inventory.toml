# What `chatddx init-data --with-giftbag` gives a new owner as their own: the
# configurations, every variation they name, and the cases. Machines, models
# and stacks stay the archive's.
extends = [
  "inventory/slices.toml",
  "inventory/configurations.toml",
  "cases.toml",
]
