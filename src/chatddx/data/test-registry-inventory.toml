# The tests' registry: inventory.toml's, without the cases. The archive's
# test inventory is it and the test cases (test-inventory.toml); as a
# giftbag, it is everything the portal shows an owner outside the Shared
# tab, given them as their own before they start (core/tests/test_seeding.py).
extends = [
  "inventory/pelle.toml",
  "inventory/malborg.toml",
  "inventory/fake.toml",
  "inventory/llms.toml",
  "inventory/clients.toml",
  "inventory/slices.toml",
  "inventory/configurations.toml",
  "inventory/scorers.toml",
]
