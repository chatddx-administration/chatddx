"""
chatddx's own tools, a file to each: all that runs when an LLM calls one.
A tool file imports nothing else of chatddx's, so the file is all of chatddx
that ran, and its git blob id names the version (`runtime.implementation`).
"""
